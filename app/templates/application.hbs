<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="#">{{#link-to 'index'}}Itscrap{{/link-to}}</a>
    </div>
    <div>
      <ul class="nav navbar-nav">
        <li><a href="#">{{#link-to 'index'}}Home{{/link-to}}</a></li>

      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><a href="#">{{search-bar search='search'}}</a></li>
      </ul>
    </div>
  </div>
</nav>
<div class='container'>
  {{#if session.isAuthenticated}}
    Logged in as {{session.currentUser.email}}
    <button class='btn btn-warning' {{action "signOut"}}>Sign out</button>
  {{else}}
    {{login-user signIn='signIn' class='button-inline'}}
    <!-- <button class='btn btn-primary button-inline'{{action "signIn" "password"}}>Sign in</button> -->
  {{/if}}
  {{outlet}}
</div>
