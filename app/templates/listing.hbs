<h1>{{model.title}} </h1>
<button class='btn btn-danger btn-xs' {{action 'deleteListing' model}}>delete</button>
<p>
  created: {{moment-from-now model.createdAt}}
</p>
<p class='content'>
  {{model.content}}
</p>
{{outlet}}
